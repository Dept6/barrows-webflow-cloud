import { defineConfig } from "@opennextjs/cloudflare";

export default defineConfig({
  // Use defaults suitable for Cloudflare build in Webflow Cloud
  // Customize if needed (e.g., outDir, routes, compatibilityFlags)
});

